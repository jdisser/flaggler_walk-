<p id="notice"><%= notice %></p>

<!-- Page head line -->
  <div class="tag1">
    <h1>Follow a trail</h1>
  </div>
  <div class="tag2">
    <h3 class="tagline2">Look up!</h3>
  </div>



<!-- Map of trails near the user -->




<!-- Start gallery of images that represent trails -->
<div class="container-fluid">
  <% @itineraries.each do |itinerary| %>
    <%= link_to image_tag(itinerary.photos.first.picture_url(:thumb)), itinerary, class:'picpin' %>
  <% end %>
</div>
<!-- End photo gallery of images that represent trails -->

    <% @itineraries.each do |itinerary| %>
        <%= link_to image_tag(itinerary.photos.first.picture_url(:thumb)), itinerary %>
        <%# link_to 'Show', itinerary %></td>
        <%# link_to 'Edit', edit_itinerary_path(itinerary) %></td>
        <%# link_to 'Destroy', itinerary, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      <% end %>






<!-- This link will not be shown on public site  -->
<%= link_to 'New Itinerary', new_itinerary_path %>
